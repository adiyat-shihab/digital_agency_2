const navigation = document.querySelectorAll(".rongin");
for (const el of navigation) {
  el.classList.add(
    "hover:text-[#FE4367]",
    "cursor-pointer",
    "transition-all",
    "duration-300"
  );
}
